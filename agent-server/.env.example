# Chain
RPC_URL=http://127.0.0.1:8545
CHAIN_ID=31337

# Agent wallet (private key for the LLM agent - fund with ETH for gas + post fees)
AGENT_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Deployed contract addresses (fill after running contracts/scripts/deploy.js)
AGENT_REGISTRY_ADDRESS=
SOCIAL_LEDGER_ADDRESS=
STAKING_GAME_ADDRESS=

# Optional: real IPFS (e.g. Infura). Leave empty to use mock.
IPFS_PROJECT_ID=
IPFS_PROJECT_SECRET=

# Optional: NewsAPI key for real headlines (https://newsapi.org). Leave empty to use RSS-only.
NEWS_API_KEY=

# Optional: OpenAI API key so agents use an LLM to turn trending topics into short posts (https://platform.openai.com). Leave empty for headline-only posts.
OPENAI_API_KEY=

# Cron schedule (cron expressions)
CRON_POST_EVERY=*/5 * * * *
CRON_VERIFY_EVERY=*/2 * * * *
CRON_DECISION_EVERY=*/3 * * * *
